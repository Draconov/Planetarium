{
  "name": "planetarium",
  "version": "1.1.0",
  "description": "Reconstructed source for the Planetarium procedural planet generator.",
  "private": true,
  "scripts": {
    "check": "node --check src/app.js && node --check src/font_data.js && node --check scripts/build-web.mjs && node --check scripts/serve.mjs && node --check scripts/collect-tauri-exe.mjs && node scripts/check-assets.mjs && node scripts/check-version.mjs",
    "dev:web": "node scripts/serve.mjs src",
    "dev:desktop": "tauri dev",
    "preview": "npm run build:web && node scripts/serve.mjs dist/web",
    "start": "npm run dev:desktop",
    "tauri": "tauri",
    "build": "npm run build:web",
    "build:web": "node scripts/build-web.mjs",
    "build:win": "npm run check && tauri build --no-bundle && node scripts/collect-tauri-exe.mjs",
    "build:all": "npm run build:win"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  },
  "engines": {
    "node": ">=20"
  }
}
